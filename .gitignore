# 独立项目 06_手机APP深度开发 —— 本地 .gitignore
# HBuilderX 构建缓存/临时产物（不随源码入独立仓库）
unpackage/
.hbuilderx/
*.log
node_modules/
# 若你希望前端资源(assets/)也由 01_源码 动态生成再同步，把这行也打开：
# assets/  (注：打开后本仓库不可独立打包，需先跑 _重建WEB并同步到APP.ps1)
