# 交接说明 · 给 DeepSeek-Harness / WorkBuddy（手机 APP 深度开发）

> 本文档供接手方快速上手：**独立 HBuilderX 5+App 移动项目**（非原生 Android），含 安卓模拟器安装 / 运行 / 适配 / 单独设计 指引。
> ⚠️ 2026-09-06 起本仓库**自包含**（frontend/ 前端源码已并入），深度开发请先读 **开发说明.md**；本文件只负责「模拟器/真机执行与检查清单」。
> 更新日期：2026-09-06 · 基底前端版本：v3.8.215（assets 内 `index-DQPq0d1K.js`）。

---

## 0. 一句话
- 这是一个**独立 git 仓库**（已 `git init` 并初始提交 `ed0cb7d`），`manifest.json` + 前端资源 `assets/` + 真题 `zhenti/` 已入库。
- 它是 **HBuilderX 5+App（WebView 壳 + 前端 Vue 资源）**，**不是原生 Android**。
- 与主仓库 `E:\公务员备考资料\行测\kaogong-review-skill-main` **完全解耦**（主仓库 `.gitignore` 忽略本目录）。

---

## 1. 目录结构
```
06_MobileApp-DeepDev/
├── README.md                 # 独立项目说明
├── 开发说明.md               # ★ 详细开发文档（架构/适配/构建/打包/实测矩阵）
├── frontend/                 # ★ 前端唯一活跃源码（Vue3+Vite+PWA；自包含可构建）
├── xingce-app-shell/     # HBuilderX 工程（manifest.json + assets + zhenti）
├── _重建WEB并同步到APP.ps1   # frontend 构建→同步壳工程→剥离壳内 PWA（唯一同步入口）
└── _交接与安卓模拟器适配指南.md # 本文档
```

## 2. 如何打开 / 跑起来
1. **HBuilderX**（装好 HBuilderX，版本 ≥ 3.x 支持 5+App）→ 文件 → 导入 → 选 `xingce-app-shell`。
2. **改逻辑**：前端功能（题目/知识库/错题等）→ 直接改本仓库 **frontend/**（`npm test` 回归）；改**外壳/打包/权限/图标** → 在本工程改 `manifest.json`。
3. **同步前端**：在仓库根运行 `powershell -ExecutionPolicy Bypass -File "_重建WEB并同步到APP.ps1"`（构建 frontend → 同步壳工程 → 剥离 PWA 残留）。

---

## 3. 安卓模拟器安装（Windows 推荐两种，任选其一）

### 方式 A（最省事）：第三方安卓模拟器（雷电 / 网易MuMu）
适合“只想跑起来看效果”：Windows 桌面安卓模拟器，自带 adb。
1. 下载安装 雷电模拟器 / 网易MuMu（任选），安装后启动。
2. 确认 adb 能连上：
   ```powershell
   adb devices      # 应列出 emulator-5554 之类
   ```
   （雷电可在设置里开启 adb 调试，或把 `adb` 路径加入 PATH；MuMu 一般自动可用。）
3. HBuilderX → **运行 → 手机或模拟器 → Android 模拟器**，选择你的模拟器即可实时预览/调试（HBuilderX 会编译并安装 5+App 壳到模拟器）。

### 方式 B：Android Studio 自带 AVD（更贴近真机/多分辨率）
1. 安装 **Android Studio** → SDK Manager 装 **Android SDK + 平台工具(platform-tools/adb)** 与某个系统镜像（如 Android 11/12/13）。
2. **Device Manager → Create Device** 选机型（如 Pixel 5 / 平板）→ 创建 AVD → 启动。
3. `adb devices` 应看到 `emulator-5554`；HBuilderX → 运行 → 手机或模拟器 → 选择该 AVD。

### 方式 C：真机
- 手机开启“开发者选项 + USB 调试”，插线后 `adb devices` 可见设备 → HBuilderX 直接“运行到手机”。

> 提示：HBuilderX 的 5+ 能力（`plus.io` / `plus.gallery` / `plus.runtime.openFile` 等）只在 **5+App 运行环境**有效。
> 若在浏览器/普通网页里测，那些“保存到相册/Download”会回退到浏览器行为；**务必用模拟器或真机 + HBuilderX 运行**来验证。

---

## 4. 适配 / 单独设计 检查清单（深度开发时逐项过）
- [ ] **已打包装到模拟器/真机**再测“导出/截图”：相册与 `Download` 路径提示、双击返回、物理返回键。
- [ ] **多分辨率/刘海/圆角**：用不同 AVD（360×800、390×844、平板 768×1280）各截一张主界面。
- [ ] **横竖屏**：5+App 锁竖屏（`manifest.json` 设置）；确认做题/资料横滑/图表可读。
- [ ] **字体缩放/大字号**：系统大字号下页面不破版（列表/雷达/表格）。
- [ ] **深色/浅色**：主题切换在模拟器两种系统模式都正常（截图跟随主题）。
- [ ] **性能**：长列表滚动、资料速算图表渲染、知识图谱 3D 在模拟器不卡（必要时降级光效）。
- [ ] **离线/弱网**：断网时 知识库速查/资料速算本地生成 可用；联网功能给出友好提示。
- [ ] **权限**：相机/相册/通知在 Android 12+ 运行时权限有处理。
- [ ] **图标/启动图**：`icon.svg` 与启动图在 `manifest.json` 配置；云打包后图标清晰。
- [ ] **WebView 内核**：Android 5+ 使用系统 WebView；建议用户在较新系统/更新 Chrome 内核下使用。

---

## 5. 深度开发建议方向（供选型）
- **外壳增强**：启动页/欢迎页、应用内更新提示、背景服务（如晨练提醒/本地通知，用 5+ push/notify）。
- **离线体验**：把知识库/真题打包（`zhenti/` 已在工程内）、本地 IndexedDB/`plus.io` 缓存作答与设置。
- **导出/分享**：`utils/downloadOut.js` 的 `plus.gallery.save` / `plus.io` 已就位——进一步做“长图拼接”或“整卷 PDF”到相册/系统分享。
- **设备适配**：平板/横屏做 双栏布局；手机保持单栏；用 CSS 断点 640/1024。
- **分包与体积**：`assets/` 较大，可把打不开的按需懒加载；观察 APK 大小（5+ 打包后通常 ~15-30MB+assets）。

---

## 6. 红线 / 约定（必须遵守）
1. **这是独立仓库**：所有改动只在本仓库 `git commit` 并推送 MobileApp-DeepDev；**不要动**主仓库 `kaogong-review-skill-main`（除非明确同步需求且先确认）。
2. **前端逻辑改动**：只改本仓库 `frontend/`，再跑 _重建WEB并同步到APP.ps1 刷新 `xingce-app-shell/`；不要直接改壳内 `assets/*.js`（下次同步会覆盖）。
3. **试用版**：试用门禁由 `VITE_TRIAL_MODE` 编译期开关控制，主仓库单独构建；本独立项目默认是**正式**产物，别混用。
4. **每步验证**：改完在模拟器/真机跑一次核心链路（对话/做题/错题/导出截图/速算），确认 5+ 能力正常。
5. **提交信息规范**：`feat/fix/chore(scope): 说明`；涉及示例可用版本文档记录到 `README`。

---

## 7. 联系方式信息（占位）
- 项目所有者：本机用户（行测AI项目）。
- 交接时请先 `git -C "06_MobileApp-DeepDev" status` 确认干净，再开工。
