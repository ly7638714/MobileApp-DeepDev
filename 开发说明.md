# 开发说明 · MobileApp-DeepDev（行测AI助手 · 安卓深度适配独立项目）

> 版本：1.0（2026-09-06）｜基底前端版本：v3.8.215
> 本仓库自包含：**前端源码(frontend/) + 5+App 壳工程(xingce-app-shell/) + 构建脚本 + 本说明**。
> 托管：https://github.com/ly7638714/MobileApp-DeepDev（独立 git 仓库，与主仓库 kaogong-review-skill-main 解耦）

---

## 0. 一句话定位

本项目把「行测AI助手」做成**非原生（Web 前端渲染）但深度适配安卓框架**的 App：
界面与业务 = Vue3 + Vite + PWA 前端（frontend/，唯一活跃源码）；
安卓宿主 = HBuilderX 5+App WebView 壳（xingce-app-shell/，云打包出 APK）。
开发策略 = **前端零重写**，把「安卓框架适配层」做深做透（返回键 / 权限 / 分区存储 / 分享 / 状态栏 / 生命周期 / 通知……）。

---

## 1. 仓库结构

    MobileApp-DeepDev/
    ├── README.md                     # 项目总览（新人先读）
    ├── 开发说明.md                    # 本文档：架构/适配/构建/打包/路线图
    ├── _交接与安卓模拟器适配指南.md     # 模拟器安装/运行/适配检查清单（执行细节）
    ├── _重建WEB并同步到APP.ps1        # 前端构建→同步壳工程→剥离壳内PWA（唯一同步入口）
    ├── frontend/                     # ★ 前端唯一活跃源码（Vue3+Vite+PWA；含测试）
    │   ├── src/                      #   App.vue 单体应用 + 模块化 utils/components/data
    │   ├── public/                   #   静态资源（图标等）
    │   ├── index.html / vite.config.js / package.json / pnpm-lock.yaml
    │   └── (dist 不入库；由脚本构建)
    ├── xingce-app-shell/             # ★ HBuilderX 5+App 壳工程（可直接 HBuilderX 打开）
    │   ├── manifest.json             #   App名/版本/权限/图标/横竖屏（安卓侧配置核心）
    │   ├── index.html / assets/ / zhenti/ / icon.svg
    │   └── unpackage/                #   HBuilderX 缓存与 APK 产物（.gitignore 忽略，不入库）

**要点**
- frontend/ 由主仓库 01_源码 于 2026-09-06 一次性导入；此后**本仓库是移动端深度开发的唯一活跃源码**，网页/小程序版本仍在主仓库独立演进，两边不再自动互相同步。
- xingce-app-shell/assets|zhenti 是**已构建快照**（保证克隆后不开前端也能 HBuilderX 直接云打包）；修改前端后必须跑同步脚本刷新快照再打包。
- unpackage/ 内是 APK 与签名证书，一律不入库不上传。

---

## 2. 技术栈与关键事实

| 项 | 值 | 说明 |
|---|---|---|---|
| 前端 | Vue 3.4(Composition) + Vite 5 + vite-plugin-pwa | 单页应用，base=./ 支持 file:// 加载 |
| 渲染/图表/3D | marked/katex/pdfjs/echarts/three/html-to-image | 已 manualChunks 分 three |
| 数据 | localStorage(60+ 个 xc_ key，storage.js 统一收口) + zhenti JSON(离线打包 3.56MB) | 已带 Quota 降级 |
| AI 调用 | OpenAI 兼容 Chat Completions(DeepSeek/视觉多模型)，Key 仅存本地 | 用户自填 |
| 备份/导出 | WebDAV；docx/md/Obsidian/截图(PNG)→相册或 Download/行测AI导出 | plus.io/gallery |
| 安卓目标 | minSdk 28(Android 9) / targetSdk 35(Android 15)，竖屏锁定 | manifest.json google.* |
| 原生触点 | nativeSave.js、downloadOut.js + ★新增 platform.js | 全项目仅此三处触碰宿主 |

**为什么这样切**：全项目 100+ 组件、200+ 单测的业务代码完全不需要知道「安卓」二字；把壳能力收口到 platform.js 一个文件后，宿主可从 5+App 平滑换成自建 WebView 宿主。

---

## 3. 架构决策与路线（已定）

### 3.1 宿主路线：先 A、预留 B
- **路线 A（当前）**：HBuilderX 5+App 云打包。优点：快、离线打包资源简单、个人自用足够；缺点：运行时为 DCloud 黑盒（WebView 内核、targetSdk 节奏、原生能力扩展受限）。
- **路线 B（演进目标）**：自建轻量 Android 宿主（Kotlin 单 Activity + System WebView + 自研 JS Bridge，或选用 Capacitor 类开源宿主）。前端只需替换 platform.js 内部实现，业务零改动。
- **不做**：uni-app x / 原生重写（现有整份 DOM/Vue 代码需推翻，成本不成比例）。

### 3.2 前端分层（收口）

    页面组件(components/*.vue) ── 业务逻辑(utils/*, store.js) ── 统一出口
       │
       └─ src/utils/platform.js   ★ 安卓/多端宿主桥（唯一允许触碰宿主的地方）
            ├─ 识别   : hostKind / isPlusHost / platformInfo
            ├─ 文件   : writeTextFile / writeBlobFile / toAbsolute / downloadsAbsRoot
            ├─ 系统   : setClipboard / getClipboard / shareText / vibrate / nativeToast / openExternal
            ├─ 返回键 : onHardwareBack / emitHardwareBack / installPlusBackBehavior(双击退出)
            └─ 信息   : statusbarHeight / runtimeVersion / exposePlatform

铁律：**业务代码禁止直接写 plus.* / window.plus**，一律经 platform.js。

---

## 4. 前端开发

### 4.1 环境与命令

    # 首次：装依赖（任选其一）
    cd frontend
    npm install          # 或 pnpm install（仓库带 pnpm-lock.yaml）

    # 日常
    npm run dev          # 浏览器热开发
    npm test             # vitest 单测（200+ 用例，含纯逻辑回归）
    npm run build        # 产物在 frontend/dist
    npm run lint / npm run format

### 4.2 风格约定（沿用 frontend/CONVENTIONS.md）
- 文件命名 kebab-case + 具名导出；目录：components / composables / utils / data / kb / api / scene。
- 持久化只走 utils/storage.js（KEYS 注册表 + safeGet/safeSet + 版本迁移），不要散写 localStorage。
- 新增纯逻辑尽量配 vitest（放 src/__tests__/）。
- 涉及宿主能力：一律 import 自 ./platform，并在第 6 节表格登记实测结果。

### 4.3 已完成的深度适配代码（v1.0）
- ★ src/utils/platform.js：新建宿主桥，全部能力 try/catch 自降级。
- src/utils/nativeSave.js、downloadOut.js：宿主探测收口到 platform.isPlusHost()（行为不变）。
- 壳工程剥离 PWA/ServiceWorker（registerSW.js/sw.js/workbox-*.js/manifest.webmanifest）——file:// 壳内不生效纯浪费；网页端 PWA 仍在 frontend 保留。

### 4.4 待接入（下一步开发项）
- [x] 物理返回键接入 App.vue（已完成：handleAndroidBack 先回退内部浮层/面板→关随手记/菜单/搜索→platform 双击退出；随下版云打包后按 §6#10 复测）
- [ ] 分享入口：截图/错题导出后加 shareText() 按钮（Android 系统分享面板）。
- [x] 剪贴板按钮统一改走 platform.setClipboard()（已完成：ChatPage 选中/整条/代码块 + FloatPanel 笔记导出共 4 处；随下版云打包复测）。
- [ ] 大对象（聊天记录等）迁 IndexedDB/OPFS，缓解 localStorage 配额。

---

## 5. 安卓壳工程（xingce-app-shell）配置说明

manifest.json 关键字段（HBuilderX 可视化与 JSON 等价）：
- name：行测名师AI小助理；version.name/code 与前端 version.js、package.json 同步递增。
- screenOrientation：portrait-primary（锁竖屏）。
- plus.distribute.android.permissions：INTERNET、ACCESS_NETWORK_STATE、CAMERA（拍照/相册选图；相机 required=false，无相机设备可装）。
- permissionPhoneState: none、permissionExternalStorage: none（Android 10+ 分区存储，导出走 plus.io 的 _downloads + plus.gallery.save，不申请存储权限——需在 Android 15 真机复核）。
- google：minSdkVersion 28 / targetSdkVersion 35。

**云打包步骤**（每轮发版）：
1. 在 frontend/ 改代码 → npm run build → 跑 _重建WEB并同步到APP.ps1（含剥离 SW）。
2. HBuilderX → 文件 → 打开目录 → 选 xingce-app-shell。
3. 发行 → 原生App-云打包 → Android → 选证书（个人用公共测试证书或自有 keystore）。
4. 下载 APK → 真机/模拟器安装验证 → 命名：行测AI助手_vX.Y.Z_日期.apk。

**红线**
- 前端逻辑只改 frontend/；壳内 assets/*.js 为生成物禁止手改（会被同步覆盖）。
- unpackage/、*.keystore、certdata、.env* 严禁提交；推送前自查 git status。
- 本仓库独立 git：所有提交只在本仓库；主仓库 01_源码/04_安卓 内容不动。

---

## 6. Android 15 模拟器适配实测（进度登记）

> 环境：本机 Windows 11 + AMD Ryzen 7 5825U（Hyper-V 层开启，走 WHPX 加速）。SDK 安装于 D:\AndroidSdk。
> 方法：API 35 (Android 15) google_apis x86_64 AVD；adb 安装 APK；逐屏截图 + 关键链路冒烟。

| # | 检查项 | 状态 | 结果/备注 |
|---|---|---|---|
| 1 | AVD 可启动、adb 可见、API=35 | ✅ | WHPX 加速；emulator-5554；release=15 / sdk=35 |
| 2 | APK 安装成功、图标名称正确 | ✅ | v3.8.215 安装成功，包名 plus.H5A0C4D12 |
| 3 | 启动页→首页渲染（无白屏/JS 报错） | ✅ | 本地账号注册/登录→看板/对话/知识库/引导均正常渲染；errorLog 有一条首启运行错误(待查) |
| 4 | 竖屏锁定、状态栏/刘海不遮内容（edge-to-edge） | ⚠️ | fullscreen:true 使状态栏隐藏(无时间/电池)，内容无遮挡；是否保留沉浸式需产品确认 |
| 5 | 设置页填 Key→对话链路（普通题） | ⏳ | 无 Key 则只验 UI/离线部分 |
| 6 | 视觉模式拍题/选图（CAMERA 运行时权限） | ⏳ | |
| 7 | 截图导出→系统相册/Download 提示 | ⏳ | 分区存储重点 |
| 8 | 文本导出/全量备份自动写 Download | ⏳ | |
| 9 | 剪贴板复制按钮 | ⚠️ | 代码已切 platform.setClipboard（4 处，5+ 下走系统剪贴板），待下版 APK 复测 |
| 10 | 物理返回键行为 | ❌ | 现版 APK 按返回键=直接退出 App(5+ 默认)；新版需 installPlusBackBehavior 接入后复测 |
| 11 | 系统分享（文本） | ⏳ | shareText 验证 |
| 12 | 深色/浅色主题、大字号不破版 | ⚠️ | 深色主题渲染正常无破版；浅色与大字号待测 |
| 13 | 离线：断网下真题/知识库/速算可用 | ⏳ | |
| 14 | 性能：长列表/图表/3D 流畅度 | ⏳ | |
| 15 | WebView 内核/UA 兼容 | ✅ | Android 15 系统 WebView 正常渲染(Vue/KaTeX/ECharts 页面可见) |

> ⏳=待测 ✅=通过 ❌=失败+原因；实测后填表并同步本文件与《_交接与安卓模拟器适配指南.md》。

### 6.1 Android 15 首轮实测记录（2026-09-06）

- 模拟器：AVD `android15`（API 35 google_apis x86_64，720×1560/320dpi，RAM 1.5G），WHPX 硬件加速，~40s 起机。
- SDK/JDK：D:\AndroidSdk（cmdline-tools 13.x / emulator 37.1.11 / platform-tools 37.0.1 / JDK 21 Temurin）。
- 测试 APK：行测AI助手_正式版_v3.8.215_20260905.apk（云打包）。
- 冒烟链路通过：安装→本地账号注册/登录→首启引导→看板/对话/知识库页面切换→截图均正常。
- 关键适配缺口（本轮实测发现）：
  1) **物理返回键直接退出 App**——对应 4.4 待接入项（onHardwareBack/installPlusBackBehavior）；
  2) **状态栏隐藏**（manifest fullscreen:true）——需确认产品意图，若显示时间/电池需改配置并复测 edge-to-edge；
  3) 对话/看板存在顶部“板块”区与倒计时，动态刷新导致 uiautomator 偶发无法 dump（仅影响自动化，不影响使用）。
- 待后续版本复测：剪贴板(9)、系统分享(11)、导出存相册(7/8)、相机运行时权限(6)、浅色/大字号(12)。
- Key 连通性实测（2026-09-06，模拟器 WebView 内，**Key 仅存模拟器未入库**）：文字 deepseek-v4-flash 200✓；视觉 deepseek-v4-flash-vision-exp 识图“HELLO 2026”✓（思考型需足够 max_tokens）；智谱 GLM-TTS(audio/speech,wav) 200 RIFF/WAVE✓。测试 Key 于 3 日后清除（清脚本在 D:\AndroidSdk\_dl\clear-keys.ps1，不随仓库）。

---

## 7. 发布与版本管理

1. 版本号三处同步：frontend/src/version.js 的 APP_VERSION、frontend/package.json 的 version、xingce-app-shell/manifest.json 的 version.name/code；CHANGELOG.md 续写变更。
2. 提交规范：feat/fix/chore(scope): 说明；涉及安卓适配用 scope: android。
3. 发版序列：开发 → 同步脚本 → 云打包 → 装 AVD/真机过第 6 节关键项 → 打 tag vX.Y.Z 并 push。
4. 备份：App 内「全量备份」写 Download；仓库侧关键产物 = 源码 + 已同步 assets，无需存 APK。

---

## 8. 风险与开放问题

- DCloud 5+ 运行时边界：内核跟随系统 WebView；无法离线自定义原生模块；云打包依赖 HBuilderX/账号。若未来上架商店或需离线私有化，应切路线 B（3.1），platform.js 已铺路。
- Android 15 强制 edge-to-edge（targetSdk 35）：状态栏/手势区渲染需真机+AVD 双向验证（第 6 节 #4）。
- 剪贴板/分享在 WebView 的限制：本版已通过 platform.js 走 Android Intent/系统剪贴板补强。
- 存储配额：localStorage 上限低，聊天等大对象后续迁 IndexedDB/OPFS（4.4）。
- 签名证书：现在用公共测试证书云打包；正式对外前需自有 keystore 并异地备份（勿入库）。

---

## 9. 接手指引（含 AI 助手 / 后续开发者）

1. 先读 README.md → 本文档第 1/3 节 → 《_交接与安卓模拟器适配指南.md》。
2. 看代码顺序：utils/platform.js → utils/storage.js → utils/downloadOut.js → App.vue（整体）→ 需要的业务模块。
3. 改前端：frontend/ 内完成并 npm test；涉及壳能力补第 6 节实测登记。
4. 安卓侧任何改动必须同时更新本文件 + manifest.json + 指南，并跑模拟器验证。
5. 不要：手改 assets、提交 unpackage/密钥/env、改动主仓库文件。